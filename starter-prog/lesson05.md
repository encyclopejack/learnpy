## Welcome back!
We’re learning two whole new function today!!

The functions are `   ` and `   `!

Haha, they’re *str* and *int*!

Have you tried ***print(1+"This will cause an error!")*** or something similar to this?

It will probably say:

<color><b>ValueError: can only concentrate 'int' to 'int'(not 'str')</b></color>

Although there is a way to Force Do this, which we’re gonna learn:

***print(str(1)+"This will not.")***

This will output **1This will not.**

The *str* method on a number will FORCE it to turn into a “sentence” which we called a “string”.

So *str(1)* is equal to *"1"*, in a more descriptive way **str is to turn number into 
string**

And you can ALSO do this:

***print(1+int(5))***

This will output ***6***.

Try doing these!

**SEE YOU NEXT CLASS!**
